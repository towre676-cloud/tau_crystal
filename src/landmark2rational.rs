// Deterministic placeholder: consumes canonicalized landmark CSV and emits 17 rationals.
// Real implementation must be SE(3)+scale invariant and LLL-backed.
// This file is a stub; audited builds must pin commit hashes.
fn main() {
    eprintln!("landmark2rational.rs: stub. Provide audited binary in Docker build.");
}
