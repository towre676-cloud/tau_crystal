# Known‑Truth Corpus

This directory anchors falsifiable claims. The period demo asserts that the declared period equals the τ‑trace sum across components. The curvature null asserts exact cancellation within numerical tolerance. The K‑class proxy asserts that two canonized boundary receipts represent the same stable class. Each claim is verified by a self‑contained script without network access, producing machine‑readable outcomes.
