[package]
name = "tau_cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tau_core = { path = "../core" }
sha2 = "0.10"
hex = "0.4"
