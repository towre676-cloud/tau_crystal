namespace Tools
namespace TauEmit

structure Dial where
  baseline : Float
  threshold : Float
deriving Repr

def emit (obs : Int) (tau : Float) (_dial : Dial) : IO PUnit := do
  IO.println s!"tau-pulse: obs={obs} at Ï„={tau}"

end TauEmit
end Tools