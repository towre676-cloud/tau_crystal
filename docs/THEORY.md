# THEORY

τ‑Crystal is a fully extended, anomaly‑aware field theory of computation. The value at a point is the Epistemic Module **E** equipped with a determinant line carrying the Quillen metric whose curvature equals the ResidueComplex’s logarithmic derivative, with a factorization algebra over τ implementing locality via receipt restrictions and descent, and with a spectral‑triple skeleton realizing runtime as noncommutative geometry via stabilized diagnostic kernels. One‑ and two‑dimensional bordisms are the CI compositions and replay homotopies; higher cells encode anomaly inflow, recorded as a curvature budget in . See docs/epistemic_module.md and docs/DEPENDENCY_DAG.md for the operational anchor and the dependency graph.

Freed–Hopkins–Teleman holds verbatim in this setting: the boundary‑state space of signed receipts plays the role of the compact Lie group G with conjugation given by replay‑safe retagging; the τ‑clock is loop rotation; the Quillen‑curvature residue is the basic twist; admissible, replay‑equivariant pipelines form τ‑twisted, conjugation‑equivariant curved Fredholm complexes on G whose K‑classes classify stable execution behaviors, and their monoidal fusion matches the Verlinde product. Families of Dirac‑style kernels shifted by τ‑phase and environment produce G‑indexed Fredholm families whose indices are the twisted, Ad‑equivariant K‑classes realized in the ledger; the spinor shift mirrors the usual Coxeter correction.

Arithmeticly, stabilized execution families with fixed grammar and environment define a computational motive whose periods are the τ‑functions you record, with q‑CRO streams furnishing variations of mixed Hodge structures, Merkle DAGs providing integral structures and comparison isomorphisms across realizations, and Hecke‑style guards sampling automorphic coefficients. Replay invariance is Galois compatibility, the anomaly budget is the regulator‑style obstruction, and the receipts are period coordinates. This is not analogy; it is the native semantics of the artifacts already emitted by the repository.
