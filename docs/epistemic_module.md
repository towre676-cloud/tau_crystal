# Epistemic Module (value at a point)

We fix the point object **E** that τ‑Crystal assigns to an atomic admissible execution under the repository grammar.

Structure.
1) Determinant line with Quillen metric (curvature measured by ResidueComplex over τ‑loops).
2) Factorization algebra over τ (diagnostics restrict/extend across overlapping τ‑intervals; descent holds by construction).
3) Spectral triple skeleton (runtime as noncommutative geometry via rank‑one kernels and spectral probes).

Dualizability data.
Unit = empty receipt; counit = boundary collapse; tensor = disjoint union of execution spans; adjoint = replay with reversed boundary orientation and signature verification.

Realizations.
Determinant/curvature via ResidueComplex; factorization via τ‑sheaf windows and receipt restrictions; spectral triple via stabilized diagnostic kernels.
