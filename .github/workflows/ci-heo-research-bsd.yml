name: HEO Research — BSD probe (optional)

on:
  push:
    branches: [ "main", "ci-**", "feat/**", "fix/**" ]
  pull_request:

jobs:
  heo-research-bsd:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Run BSD probe (non-blocking)
        run: |
          chmod +x ci/heo/research/*.py
          python ci/heo/research/bsd_lfunction_test.py
