name: holonomy-align
on:
  push:
  workflow_dispatch:
    branches-ignore: [ ci-spine-clean-* ]
jobs:
  guard:
    if: !startsWith(github.ref_name, 'ci-spine-clean-')
    runs-on: ubuntu-latest
    steps:
      - run: echo 'skipping on ci-spine-clean-*'
